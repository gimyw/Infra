{{- if .Values.ai.enabled }}
# AgentCore 파드용 SA (IRSA: Bedrock·Lambda·S3 권한)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.ai.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.ai.roleArn }}
{{- end }}
