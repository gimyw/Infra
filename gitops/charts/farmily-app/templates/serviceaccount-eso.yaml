# ESO 가 Secrets Manager 를 읽을 때 쓰는 SA (IRSA: eso-reader)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-reader
  namespace: {{ .Values.namespace }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.eso.roleArn }}
