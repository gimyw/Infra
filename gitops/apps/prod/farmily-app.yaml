apiVersion: v1
kind: ServiceAccount
metadata:
  name: farmily-app
  namespace: farmily-prod
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::851957594139:role/prod-eks-app-role
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: farmily-app
  namespace: farmily-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: farmily-app
  template:
    metadata:
      labels:
        app: farmily-app
    spec:
      serviceAccountName: farmily-app
      containers:
        - name: farmily-app
          # prod ECR 이미지. latest 대신 배포 시점 태그/digest 핀 권장(롤백 추적).
          image: 851957594139.dkr.ecr.ap-northeast-2.amazonaws.com/farmily-api:latest
          ports:
            - containerPort: 8080
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: "prod" }
            - { name: APP_VARIANT, value: "eks" }          # ★ 버전 마커(ECS vs EKS 식별)
            - { name: S3_REGION, value: "ap-northeast-2" }
            - { name: AGENTCORE_URL, value: "http://farmily-app-ai:8080" }
          envFrom:
            - secretRef:
                name: app-secrets   # ESO 가 farmily/prod/app + app-infra 병합 생성
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: farmily-app
  namespace: farmily-prod
spec:
  selector: { app: farmily-app }
  ports: [ { port: 8080, targetPort: 8080 } ]
